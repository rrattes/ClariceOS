---
# Runs in the chroot of the newly installed system.
# Detects the installed desktop environment and configures
# the appropriate display manager and session settings.
dontChroot: false
timeout: 600

script:
  - "/etc/calamares/scripts/configure-de.sh"
