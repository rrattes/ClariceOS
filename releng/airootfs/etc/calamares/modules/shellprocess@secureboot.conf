---
# Prepares Secure Boot keys and signs EFI binaries on the installed system.
# Safe to run on non-UEFI or systems without sbctl — exits gracefully.
dontChroot: false
timeout: 120

script:
  - "/etc/calamares/scripts/setup-secureboot.sh"
