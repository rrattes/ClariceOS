---
# ClariceOS — remove live-session packages after installation.
# Replaces the built-in 'packages' module to avoid `pacman -Rs` failing
# the entire installation when a package is absent or has dep conflicts.

timeout: 120

script:
  - "-/etc/calamares/scripts/remove-live-pkgs.sh"
