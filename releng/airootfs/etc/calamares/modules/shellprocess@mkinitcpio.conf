---
# Runs mkinitcpio in the target chroot to generate the initramfs image.
# Replaces the built-in initramfscfg + initramfs modules which are not
# available in the AUR build of Calamares.
dontChroot: false
timeout: 120

script:
  - "/usr/bin/mkinitcpio -P"
