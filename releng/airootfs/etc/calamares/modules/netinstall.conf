---
# Desktop environment selection using a local YAML file.
# The show phase presents the DE choice UI.
# The exec phase installs packages for the selected DE.
groupsUrl: file:///etc/calamares/netinstall.yaml

# Don't block installation if internet is unavailable —
# GNOME is pre-installed offline. KDE requires an internet connection.
required: false
