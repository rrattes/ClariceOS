---
# Generates limine.cfg and configures btrfs/snapper pieces inside chroot.
# Raw Limine deployment to disk/ESP runs in shellprocess@limine-deploy.
dontChroot: false
timeout: 180

script:
  - "/etc/calamares/scripts/install-bootloader.sh"
