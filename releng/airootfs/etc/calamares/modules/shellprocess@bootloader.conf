---
# Installs Limine as the bootloader for the newly installed system.
# If btrfs was chosen as the root filesystem, also sets up
# snapper and limine-snapper-sync for automatic snapshot boot entries.
dontChroot: false
timeout: 180

script:
  - "/etc/calamares/scripts/install-bootloader.sh"
