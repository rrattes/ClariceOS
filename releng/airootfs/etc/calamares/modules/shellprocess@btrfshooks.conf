---
# Ensures the btrfs mkinitcpio hook is present when root filesystem is btrfs.
# Runs after initramfscfg (which writes mkinitcpio.conf) but before initramfs
# (which runs mkinitcpio to build the initramfs image).
dontChroot: false
timeout: 30

script:
  - "/etc/calamares/scripts/btrfs-hooks.sh"
